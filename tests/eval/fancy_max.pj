fn fancy_max(gt: Int -> Int -> Bool, x: Int, y: Int) do
    if gt(x, y) do
        x
    else
        y
    end
end

fn regular_gt(x: Int, y: Int) do
    (x - y) > 0
end

fancy_max(regular_gt, 10, -10)
